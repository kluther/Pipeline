<?php$project = $SOUP->get('project');$task = $SOUP->get('task');//$user = $SOUP->get('user');//$updates = $SOUP->get('updates');//$accepted = $SOUP->get('accepted');$fork = $SOUP->fork();$fork->set('project', $project);$fork->set('pageTitle', $project->getTitle());$fork->set('headingURL', Url::project($project->getID()));$fork->set('selected', "tasks");$fork->set('breadcrumbs', Breadcrumbs::updateNew($task->getID()));$fork->startBlockSet('body');?><div class="left"><?php	$SOUP->render('project/partial/updateNew', array(		));?></div><div class="right"><?php	$SOUP->render('project/partial/tasks', array(		'tasks' => array($task),		'size' => 'small',		'title' => 'Task Info',		'hasPermission' => false	));?><?php	$SOUP->render('project/partial/updates', array(		'size' => 'small',		'title' => 'Your Task Updates',		'taskUpdates' => true,		'hasPermission' => false	));?></div><?$fork->endBlockSet();$fork->render('site/partial/page');