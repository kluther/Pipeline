<?phpinclude_once TEMPLATE_PATH.'/site/helper/format.php';$creator = $SOUP->get('creator');$fork = $SOUP->fork();$fork->set('title', 'Creator');$fork->startBlockSet('body');?><ul class="segmented-list users">	<li>		<?= formatUserPicture($creator->getID(), 'small') ?>		<h6 class="primary"><?= formatUserLink($creator->getID()) ?></h6>		<p class="secondary">creator</p>	</li></ul><?php$fork->endBlockSet();$fork->render('site/partial/panel');